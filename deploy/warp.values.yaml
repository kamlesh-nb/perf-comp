deployment:
  image: "taugem/warpapi"
  tag: "0.1.0"
  name: "warp-api-deployment"
  label: "warp-api"
  limits:
    cpu: 10m
    memory: 4Mi
  requests:
    cpu: 25m
    memory: 12Mi

service:
  name: "warp-api-service"
  type: LoadBalancer
  port: 8080
  targetPort: 80

hpa:
  maxReplicas: 10  
  minReplicas: 1  
  targetCPUUtilizationPercentage: 20