apiVersion: apps/v1
kind: Deployment
metadata:
  name: gofiber-api-deployment
  labels:
    app: gofiber-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gofiber-api
  template:
    metadata:
      labels:
        app: gofiber-api
    spec:
      containers:
      - name: gofiber-api
        image: gofiberapi:0.1.0
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "32M"
            cpu: "250m"
          limits:
            memory: "56M"
            cpu: "500m"          
